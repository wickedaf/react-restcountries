import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div className="App-header">
            <h1>React RestCountries</h1>
        </div>
    );
};

export default Header;